import {assert, expect} from 'chai';
import sinon from 'sinon';
import * as scrollListener from '../../../script/util/listeners/scrollListener';

describe('scrollListener', function () {

    // TODO: scrollListener.js is not a very testable file at the moment, these are just stubs.

    describe('bindContentScrollListener', function () {
        it('should be written to be more testable', function () {
        });
    });

    describe('bindPageScrollListener', function () {
        it('should be written to be more testable', function () {
        });
    });
});
